/**
 * @author 
 * @kind module
 * @desc databasehandler
 * @exports db
 */

// Figure out a good way to structure models here.
// Probably want this file to require all of the different models, and handle the db connection.
// then in index.js in / can simply require this file.

var connectionString = "yadayada";

module.exports = db;